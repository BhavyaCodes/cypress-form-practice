import { Box } from "@mui/material";
import React from "react";

interface StepProps {
  stepNumber: string;
  active: boolean;
}

const Step: React.FC<StepProps> = ({ stepNumber }) => {
  return <Box></Box>;
};

export default Step;
